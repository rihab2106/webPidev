<form method="post" action="{{ path('app_usergroups_delete', {'idUsersGps': user_group.idUsersGps}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user_group.idUsersGps) }}">
    <button class="btn">Delete</button>
</form>
