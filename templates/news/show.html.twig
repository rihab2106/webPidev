{% extends 'back/BackBase.html.twig' %}

{% block title %}Comments
{% endblock %}

{% block content %}
	<h1>News And Comments</h1>

	<table class="table">
		<tbody>
			<tr>
				<th>Id</th>
				<td>{{ news.idNews }}</td>
			</tr>
			<tr>
				<th>Headline</th>
				<td>{{ news.headline }}</td>
			</tr>
			<tr>
				<th>Content</th>
				<td>{{ news.content }}</td>
			</tr>
			<tr>
				<th>Img</th>
				<td>{{ news.img }}</td>
			</tr>
		</tbody>
	</table>
	<table class="table">
		<thead>
			<tr>
				<th>Comment</th>
				<th>Likes</th>
				<th>Dislikes</th>
				<th>Action</th>

			</tr>
		</thead>


		<tbody>
	{# 	<button>
				<a href="{{ path('add_comment'),{'id': news.idNews} }}"> Add a comment</a>
                
			</button> #}

            <button class="btn btn-light">
            <a href="{{ path('add_comment', {'news': news.idNews}) }}">Add a Comment</a>
            </button>


			{% for c in com %}
				<tr>

					<td>{{ c.comment }}</td>
					<td>{{ c.likes }}</td>
					<td>{{ c.dislikes }}</td>
					<td>
						<a href="{{ path('app_comments_edit', {'idComment': c.idComment}) }}">Update or Delete</a>
					</td>
				</tr>
			</a>
		</tbody>

	</td>


</tr>{% else %}

<tr>
	<td colspan="5">no records found</td>
</tr>{% endfor %}
<br>
<a href="{{ path('app_news_index') }}">Back To News</a>
<br>
<a href="{{ path('app_news_edit', {'idNews': news.idNews}) }}">Edit</a>
<br>
{{ include('news/_delete_form.html.twig') }}{% endblock %}
