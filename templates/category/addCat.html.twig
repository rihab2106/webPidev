{% extends "back/BackBase.html.twig" %}

{% block form %}
    {{ form(fo) }}
{% endblock %}
{% block content %}
    <div class="card">
        <div class="card-body">
            <div class="p-4 border rounded">
                <div class="was-validated">


                  {{ form_start(fo) }}
                    {{ form_label(fo.category,"Category") }}
                    {{ form_widget(fo.category, {'attr': {'class': "form-control is-invalid", "required": "true"}}) }}
                    {{ form_end(fo) }}

                </div>
            </div>
        </div>
    </div>
{% endblock %}
{% block title %}

{% endblock %}

