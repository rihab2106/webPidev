{% extends 'base.html.twig' %}


{% block content %}
    {#<script>

        $("#checkAll").click(function () {
            $('input:checkbox').not(this).prop('checked', this.checked)
        });
        $("#delete").click(function() {
            $.ajax({
                url: "/deleteTrophies",
                method: "POST",
                dataType: "json",
                data: {"trophies": {{ trophies }}}
                async: true,
                success: function (data, status){
                    {% set trophies=data %}
                },
                error: function(xhr, textStatus, errorThrown) {
                    alert('Ajax request failed.');
                }
            })
        })
    </script>#}
    <div class="" >
        <div class="nk-main">

            <div class="nk-table">
                {#
                <a href="{{ path('deleteCompetitions') }}" id="delete"><button class="btn btn-light buttons-copy buttons-html5"
                         type="submit"><span>Delete</span></button>
                    </a>#}
                <a href="{{ path('addCompetitionsFront') }}" ><button class="nk-btn nk-btn-rounded nk-btn-color-dark-3 nk-btn-hover-color-info">
                        <span>Add Competitions</span></button>
                </a>
                <hr>
                <table id="example2" class="table table-striped table-bordered">
                    <div  id="example2_wrapper" class="dataTables_wrapper dt-bootstrap5">
                        <thead>
                        <tr>


                            <th>Id Competition</th>
                            <th>Game Name</th>
                            <th>Date of Competition</th>
                            <th>Delete</th>
                            <th>Edit</th>

                        </tr>
                        </thead>
                        <tbody>
                        {% for c in competitions %}
                            <tr>

                                <td>{{ c.idCompetion }}</td>
                                <td>{{ c.gameName }}</td>
                                <td>{{ c.dateofcomp|date }}</td>

                                <td><a href="{{ path('deleteCompetitionsFront', {'id': c.idCompetion}) }}">
                                        <button CLASS="nk-btn nk-btn-rounded nk-btn-color-dark-3 nk-btn-hover-color-info">Delete</button></a></td>
                                <td> <a href="{{ path('updateCompetitionsFront', {'id': c.idCompetion}) }}">
                                        <button CLASS="nk-btn nk-btn-rounded nk-btn-color-dark-3 nk-btn-hover-color-info">Modify</button></a> </td>
                            </tr>
                        {% endfor %}
                        </tbody>

                    </div>
                </table>
            </div>
        </div>
    </div>
    </div>
{% endblock %}
{% block title %}

{% endblock %}

