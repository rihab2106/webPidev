<form method="post" action="{{ path('app_comments_delete', {'idComment': comment.idComment}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ comment.idComment) }}">
    <button class="btn">Delete</button>
</form>
